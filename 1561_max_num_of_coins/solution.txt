public int maxCoins(int[] piles) {
        Arrays.sort(piles);
        int l = piles.length;
        int sum=0;
        for(int i=1;i<piles.length;i+=3){
            sum+=piles[i];
        }
        return sum;
        
    }
