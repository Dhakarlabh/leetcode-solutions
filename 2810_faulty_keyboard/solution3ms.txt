public String finalString(String s) {
        StringBuilder str = new StringBuilder();
        for(char c: s.toCharArray()){
            if(c == 'i'){
                str.reverse();
            }else {
                str.append(c);
            }
        }
        return str.toString();
    }
